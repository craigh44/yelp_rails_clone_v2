<%= form_for @restaurant do |f| %>
	<%= f.label :name, :autocomplete => :off %>
	<%= f.text_field :name, :autocomplete => :off %><br/>
	<%= f.label :description, :autocomplete => :off %>
	<%= f.text_field :description, :autocomplete => :off %><br/>
	<%= f.submit %>
<% end %>